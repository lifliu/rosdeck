<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>omni_mission_manager</name>
  <version>0.1.0</version>
  <description>
    Mission Manager for the omni inspection robot (V1): serves the
    ExecuteInspection action, MissionControl and ListRoutes services,
    drives the planner via the FollowRoute action, holds the mission
    control lease, and persists the mission/event history in SQLite with
    restart recovery (active missions become INTERRUPTED, never
    auto-resumed).
  </description>
  <maintainer email="maintainer@example.com">Rosdeck Maintainers</maintainer>
  <license>Apache-2.0</license>

  <depend>rclpy</depend>
  <depend>action_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>std_msgs</depend>
  <depend>omni_robot_interfaces</depend>

  <test_depend>python3-unittest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>